# v0.2.3 执行任务清单

> 状态：执行中；B1 主机加固已实施，B2 L1 三时段门禁通过
>
> 重编日期：2026-09-14
>
> 需求基线：[CHANGES-v0.2.3.md](./CHANGES-v0.2.3.md)
>
> 工作分支：`release-v0.2.3`；运行基线：`v0.2.2`

## 1. 范围与完成定义

本版本按以下顺序完成：

1. 盘点并加固上海服务器；
2. 从上海和新加坡分别测量到 Supabase 新加坡区的数据库延迟；
3. 延迟门禁通过后，将应用部署到上海并完成真实接口、下单事务和并发扣料复测；
4. 完成备案域名、HTTPS 与外部平台地址切换；
5. 形成验收报告、回滚说明和发布记录。

本版本不修改数据库结构，不实施 `TECH_SPEC.md` §13.2 的下单存储过程重构，不切换支付宝生产通道。X3 测试商品属于并行运营数据，不扩大基础设施版本范围。

任务状态：`[ ]` 未开始，`[~]` 进行中或等待外部条件，`[x]` 已完成且有证据，`[-]` 经确认跳过。

## 2. 当前基线

- [x] 以 `master@3c83fdec` / Tag `v0.2.2` 为代码基线创建 `release-v0.2.3`。
- [x] v0.2.3 需求已重定为“境内迁移与跨境延迟验证”，原支付宝实付验证方案作废。
- [x] 上海目标服务器已提供：`124.71.145.245`，2 核 2G，支持 ICP 备案。
- [x] Supabase 保持新加坡 `ap-southeast-1`；生产连接要求使用 Supavisor `6543` transaction 模式。
- [x] 旧氛围灯已软删除；阅星瞳 X3 机型及可行性测试商品已创建并通过线上检查，提交为 `e9c9b11`、`d25ba5f`。
- [x] 上海服务器的系统版本、磁盘、网络、主机防火墙、开放端口和已有服务已核验；云安全组仍待控制台确认。
- [ ] 上海服务器尚未安装部署组件，尚未写入应用环境变量，也尚未承载业务流量。
- [ ] 备案域名、ICP 备案号、正式 DNS 切换时间尚未确定。

## 3. 批次与门禁

| 批次 | 工作内容 | 完成门禁 |
|---|---|---|
| B0 | 文档重编与安全准备 | 清单获审阅；目标、凭据边界和回滚原则明确 |
| B1 | 上海服务器盘点与基础加固 | 密钥登录验证；SSH/UFW/Fail2ban/更新策略验收；无远程锁死 |
| B2 | T160-L1 裸数据库延迟 | 上海与新加坡三时段数据完整；给出 p50/p95/p99 和门禁结论 |
| B3 | T161 部署能力与低内存发布链路 | 2G stop-start 发布路径可用；回滚演练通过 |
| B4 | T160-L2/L3 应用与下单延迟 | 五类接口、下单事务、10 抢 5 并发与对照组完整 |
| B5 | T161 上海迁移与观察 | 上海健康检查通过；版本一致；日志无持续异常 |
| B6 | T162 域名、HTTPS 与备案配合 | DNS、证书、构建地址、回调及 Supabase Auth 地址一致 |
| B7 | 全链路验收与版本收尾 | 所有门禁通过；报告、Release Notes、回滚说明完成 |

每个批次独立验证和提交。B2 延迟结论不满足门禁时，不进入 B3–B6，先输出替代方案供决策。

## 4. B0：安全准备与可恢复性

- [x] B0-01：确认本清单与 `CHANGES-v0.2.3.md` 一致，旧支付宝任务不再作为当前待办。
- [x] B0-02：核对仓库、文档和 Git diff 不含服务器密码、数据库连接串、Supabase Secret/Service Role 或私钥。
- [~] B0-03：把初始密码视为临时凭据；SSH 密钥登录和禁用密码登录已完成，密码轮换待用户密码管理器接管。
- [~] B0-04：已记录变更前 SSH 摘要并建立主机内 root-only 恢复点；华为云快照和安全组待控制台确认。
- [x] B0-05：已核验本机到上海、新加坡服务器的 SSH 主机指纹，发现变化时停止连接并人工核对。
- [x] B0-06：采样时段采用约 10:00、当前下午和约 21:00，所有结果统一记录 `Asia/Shanghai`。
- [x] B0-07：原始采样数据只保留脱敏主机标识、时间和耗时，不记录连接串或查询结果中的业务数据。

## 5. B1：上海服务器盘点与基础加固

### 5.1 只读盘点

- [x] B1-01：记录云厂商/区域、系统发行版、内核、CPU、内存、Swap、磁盘和时区。
- [x] B1-02：检查公网/内网地址、DNS、出站访问、路由、连接 MTU 和到 Supabase 连接池端口的 TCP/TLS 可达性。
- [~] B1-03：已检查监听端口、UFW 和当前进程；华为云安全组仍待控制台确认。
- [x] B1-04：检查 SSH 有效配置、root 密钥、密码登录、登录失败记录与已授权公钥。
- [x] B1-05：检查 Docker、Nginx、Fail2ban、unattended-upgrades、Node.js、PostgreSQL 客户端及系统更新状态。
- [x] B1-06：确认无挖矿进程；来源待确认的旧 root 公钥已移出活动授权列表，原值保留在恢复点。

### 5.2 加固实施

- [x] B1-07：先安装执行方 SSH 公钥，并在第二个独立会话验证可登录；验证后才关闭密码登录。
- [x] B1-08：已启用 unattended-upgrades、安装积压安全更新并受控重启；新内核 5.15.0-191 已生效。
- [x] B1-09：应用 SSH 加固：禁止 root 密码登录、关闭交互式密码/X11/转发、限制重试与空闲会话。
- [x] B1-10：启用 UFW 默认拒绝入站；开放 22/80/443，并按用户要求增加现有服务 8888/5177；5003 当前未开放。
- [x] B1-11：已启用 Fail2ban、内核 sysctl、journald 限额与 Docker 日志轮转。
- [x] B1-12：校验 `sshd -t`、UFW、Fail2ban 和系统服务；再次从独立会话验证 SSH，确认没有锁死。
- [~] B1-13：SSH 初始密码登录已禁用；密码本身待用户通过密码管理器轮换或确认锁定。
- [x] B1-14：已形成 `docs/ops/SHANGHAI-SERVER-HARDENING.md`，记录加固差异和遗留风险，不含密钥内容。

## 6. B2：T160-L1 裸数据库延迟

### 6.1 测量工具

- [x] B2-01：新增只执行 `SELECT 1` 的测量脚本，强制校验目标端口为 `6543`，设置连接/语句超时。
- [x] B2-02：冷连接与热连接分开统计；两地三个时段、每种连接方式均完成 100 个有效样本。
- [x] B2-03：计算 p50/p95/p99、最小/最大值、成功率和超时数；禁止只报平均值。
- [x] B2-04：原始 JSON 使用时间戳和地点命名；凭据通过 0600 临时文件传入，采样归档后已安全删除。

### 6.2 上海与新加坡对照采样

- [x] B2-05：上海服务器上午完成冷热连接采样。
- [x] B2-06：上海服务器下午完成冷热连接采样。
- [x] B2-07：上海服务器晚高峰完成冷热连接采样。
- [x] B2-08：现有新加坡服务器在三个对应时段均完成同参数对照采样。
- [~] B2-09：已记录实例、线路、任务退出状态和错误率，并确认无构建/安全更新重叠；系统未启用历史 CPU/内存记账，该项限制已在报告披露。
- [x] B2-10：复核数据库连接确实指向目标 Supabase 项目及 Supavisor transaction 池；本地 `.env.dev` 的 5432 被脚本主动拒绝，未计入结果。

### 6.3 L1 结论门禁

- [x] B2-11：`docs/ops/LATENCY-BASELINE.md` 已记录三时段冷热连接 p50/p95/p99 及跨境净增量。
- [x] B2-12：三时段上海热连接 p95 均 `<80ms`，L1 结论为正常，允许进入部署和 L2/L3 验证。
- [-] B2-13：L1 未触发暂停迁移门禁，本版本无需输出两种替代方案对比。

## 7. B3：T161 部署能力与低内存发布链路

- [x] B3-01：L1 通过后安装 Docker 29.8.0、Compose 5.5.1，并核验既有 Nginx 1.26.3。
- [x] B3-02：新增 2 核 2G 专用低内存 stop-start 发布脚本，不复用双容器蓝绿切换。
- [x] B3-03：脚本强制不可变 `sha-<commit>` 镜像、先拉取后停旧，并通过健康失败自动恢复演练。
- [x] B3-04：已核验容器 CPU/内存/PID、日志、只读文件系统和 `127.0.0.1:3000` 绑定。
- [x] B3-05：已创建 `/opt/3d-printer-site`，部署文件归 root，运行时 `.env` 权限为 0600。
- [x] B3-06：已安全写入上海运行时环境；数据库使用 6543 transaction，Secret/Service Role 仅在服务端文件。
- [~] B3-07：公开 GHCR 访问认证成功但境内直拉严重限速；本次用校验哈希的一次性中继完成，长期境内 Registry 待补。
- [x] B3-08：已用不可变基线镜像完成本机健康检查与强制失败自动回滚演练，未切换公网 DNS。
- [x] B3-09：已记录首次健康等待 17.067 秒、回滚结果、稳态内存和磁盘占用；内核不提供容器历史 `memory.peak`。

## 8. B4：T160-L2/L3 真实应用与下单验证

### 8.1 L2 五类接口

- [ ] B4-01：分别测量商品列表、X3 商品详情、实时库存、购物车和 X3 机型页。
- [ ] B4-02：每类接口预热后采集足够样本，记录端到端耗时、`Server-Timing` 与 >500ms 慢请求日志。
- [ ] B4-03：在新加坡现网或同镜像环境执行相同请求作为对照，计算跨境净增量。
- [ ] B4-04：确认响应内容、状态码、缓存命中与数据库查询行为一致，不能用缓存页面代替实时接口测量。

### 8.2 L3 下单事务与并发扣料

- [ ] B4-05：使用隔离的测试用户、地址、购物车、SKU 和耗材，记录初始库存；不触发真实付款。
- [ ] B4-06：完整执行加购 → 订单试算 → 创建订单，记录事务 p50/p95/p99 与错误率。
- [ ] B4-07：复测“10 个并发订单争抢 5 份可售库存”，必须恰好 5 成功、5 返回 40901、无超卖。
- [ ] B4-08：核对失败订单未残留库存预占、订单项或重复流水；成功订单通过正常取消/超时流程释放预占。
- [ ] B4-09：在新加坡环境执行同参数对照，量化事务和锁等待净增量。
- [ ] B4-10：把 L2/L3 数据补入延迟报告并明确判断：下单 p95 `<800ms` 可接受；`800ms–2s` 建议改造；`>2s` 或并发失败必须暂停迁移。
- [ ] B4-11：定点清理临时测试数据和受限采样文件，保留必要审计与脱敏统计。

## 9. B5：T161 上海迁移与观察

- [ ] B5-01：确认 L1、L2、L3 门禁全部允许迁移，并建立旧服务器和上海服务器部署恢复点。
- [ ] B5-02：在约定低峰窗口通过 stop-start 链路发布目标 SHA 镜像。
- [ ] B5-03：在服务器本机验证 `/api/health` 返回 `status=ok`、`database=ok`，版本与镜像 SHA 一致。
- [ ] B5-04：通过临时 IP/Host 方式验证首页、商品、X3 机型、登录、购物车、结算、后台和 Cron；不提前改正式 DNS。
- [ ] B5-05：检查容器健康、内存/CPU、磁盘、连接池、慢请求和错误日志，连续观察至少 30 分钟。
- [ ] B5-06：验证重启后容器自动恢复、Nginx 代理正常、敏感文件权限不变。
- [ ] B5-07：若健康或性能门禁失败，恢复上一镜像或停止上海迁移，不影响新加坡现网。

## 10. B6：T162 域名、HTTPS 与备案配合

- [ ] B6-01：用户确认备案域名、域名持有人、接入商备案要求和 ICP 展示文本；未确认前不构建正式域名镜像。
- [ ] B6-02：配置上海 Nginx vhost、HTTPS 证书、HTTP → HTTPS 跳转及安全响应头。
- [ ] B6-03：以正式域名重建镜像，使 `NEXT_PUBLIC_SITE_URL` 进入浏览器构建产物；只重启容器不算完成。
- [ ] B6-04：同步更新 Supabase Auth Site URL / Redirect URLs、GitHub Variables 和允许的回调地址。
- [ ] B6-05：企业支付宝获批后再更新 `ALIPAY_NOTIFY_URL` / `ALIPAY_RETURN_URL`；本版本不切换生产支付凭据。
- [ ] B6-06：降低 DNS TTL，切换前核对上海公网健康；切换后检查解析、证书链、IPv4/IPv6 和 Cloudflare 模式。
- [ ] B6-07：确认 canonical、sitemap、JSON-LD 和 `_next/static` 只包含新 HTTPS 域名，不含旧域名、IP 或 `http://`。
- [ ] B6-08：确定旧域名策略：保留回滚窗口后做 301，避免两个域名长期输出重复内容。
- [ ] B6-09：在页脚展示 ICP 备案号及经营主体信息；备案未完成时遵守接入商对网站开放状态的要求。
- [ ] B6-10：记录 DNS、Nginx、镜像与 Supabase Auth 的逐项回滚步骤。

## 11. B7：最终验收与版本收尾

- [ ] B7-01：执行 `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build`、`pnpm security:audit`。
- [ ] B7-02：执行项目 acceptance，并人工回归首页、X3 商品、机型页、注册/登录、购物车、结算、订单、后台和定时任务。
- [ ] B7-03：核对上海服务器 SSH、UFW、Fail2ban、系统更新、Docker/Nginx、资源限制与日志轮转。
- [ ] B7-04：完成 `docs/ops/LATENCY-BASELINE.md` 与 `docs/v0.2.3/ACCEPTANCE-v0.2.3.md`。
- [ ] B7-05：更新 `README.md`、`OPEN-DECISIONS.md` 和部署文档，写明最终服务器、域名、数据库区域、发布方式与回滚入口。
- [ ] B7-06：生成 `docs/v0.2.3/RELEASE_NOTES.md`，记录延迟结论、迁移结果、停机窗口与遗留风险。
- [ ] B7-07：执行凭据扫描和 Git diff 审查，确保无密码、私钥、连接串或测试个人信息。
- [ ] B7-08：用户确认后合并 master、发布 `v0.2.3` Tag；Tag 前再次核对线上健康版本。

## 12. 最终验收矩阵

| 验收项 | 最低证据 | 状态 |
|---|---|---|
| 上海服务器加固 | 加固前后摘要、密钥二次登录、UFW/Fail2ban/SSH 有效配置 | [ ] |
| L1 裸延迟 | 两地 × 三时段 × 冷热连接，均含 ≥100 样本和 p50/p95/p99 | [ ] |
| L2 接口延迟 | 五类接口、Server-Timing、慢请求与新加坡对照 | [ ] |
| L3 下单延迟 | 完整下单 p50/p95/p99 与错误率 | [ ] |
| 并发正确性 | 10 抢 5：5 成功、5 个 40901、无超卖或脏预占 | [ ] |
| 低内存发布 | stop-start 成功、健康失败可回滚、停机时长已记录 | [ ] |
| 上海应用可用 | 本机及公网健康检查、版本 SHA、30 分钟资源与错误观察 | [ ] |
| 域名与 HTTPS | DNS、证书、跳转、构建地址、canonical/sitemap/JSON-LD 一致 | [ ] |
| 外部平台配置 | Supabase Auth、GitHub Variables、后续支付宝回调地址一致 | [ ] |
| 安全与脱敏 | 仓库、日志、报告无敏感凭据或个人信息 | [ ] |
| 版本收尾 | Acceptance、Release Notes、README、回滚说明与 Tag | [ ] |

## 13. 审阅时仍需确认的输入

1. 三个延迟采样时段的具体时间；建议覆盖上午、下午和 21:00 左右晚高峰。
2. 华为云安全组当前开放端口，以及 5003 是否需要作为临时 IP 验收入口。
3. 最终备案域名、域名持有人和 ICP 接入商流程。
4. DNS 切换及可接受停机窗口；2 核 2G 使用 stop-start 会有数十秒停机。
5. 旧新加坡服务器在切换后保留多久作为回滚节点。

## 14. 禁止事项

- 不把服务器密码、SSH 私钥、数据库连接串、Supabase Secret/Service Role 写入 Git、报告或命令参数。
- 不在 SSH 公钥独立验证前关闭密码登录。
- 不以 5432 直连、`ping` 或单一时段平均值替代规定的 6543 三时段测量。
- 不在 L1/L3 门禁未通过时迁移公网流量。
- 不在 2 核 2G 上海服务器上强行同时运行两套应用容器。
- 不为性能测试伪造已支付订单，不直接改库存数值制造并发结果。
- 不在域名未确认时构建正式发布镜像，也不把只重启容器当作站点地址已更新。
