# 未决事项与决策留痕

> 跨版本的决策登记表。每条要么有结论（已决），要么有明确的归属版本（未决）。
> 讨论中出现过但没写进任何变更文档的决定，都应登记在这里，避免下次重新讨论。
> 最后更新：2026-09-17

---

## A. 已决（不要重新讨论）

| # | 事项 | 结论 | 定于 |
|---:|---|---|---|
| A1 | 目标客户 | 先中文用户跑通，国际化推迟；架构预留支付 Provider 扩展位 | 09-08 |
| A2 | 生产服务器规格 | 华为云 Flexus L 新加坡 **2 核 4 GiB / 60 GiB**；旧 2C2G 降级为 staging，不退订 | 09-08 |
| A3 | 操作系统 | **Ubuntu 24.04 LTS** | 09-08 |
| A4 | 宝塔面板 | 新机器**不安装**；Nginx 用 apt，证书用 certbot/acme.sh | 09-08 |
| A5 | 业务邮件与登录 OTP | **发信通道分离**，独立子域（`auth.` 与 `notify.`），各自 SPF/DKIM/DMARC | 09-08 |
| A6 | 邮件发送与订单事务 | **不进事务**：事务内只写 `email_messages(pending)`，cron 异步发送，幂等靠 `dedupe_key` 唯一索引 | 09-08 |
| A7 | 用户端售后形态 | **申请单 → 后台审核 → 退款**，不允许客户端直接触发退款 | 09-08 |
| A8 | 退款优惠分摊 | **按商品小计比例分摊优惠**。全部退完时累计退款额恰等于实付金额 | 09-09 |
| A9 | 退款与运费 | 部分退款**不退运费**；全部退完时**连运费一起退**；跌破包邮门槛**不补收运费** | 09-09 |
| A10 | 客户可申请退款的边界 | 仅 `print_job.status = queued` 允许；进入 `printing` 起不允许。**后台管理员不受限制** | 09-09 |
| A11 | 运营文案维护方式 | **走语言包 + 代码发布**，不扩展后台 settings 管理界面（后续不会频繁改文案） | 09-09 |
| A12 | 品牌文案定稿 | 不单独占用版本号，作为 v0.2.2 首页改造的一部分；清单见 `docs/v0.2.2/BRAND-COPY.md` | 09-09 |
| A13 | `CHANGES-v1.1.md` | **视为不存在**。3D 换色预览期将来全新起草 | 09-09 |
| A14 | v0.2.1 验收 | **已关闭**，P0 清单 H01–H17 全部完成 | 09-09 |
| A15 | 首页内容边界 | 恢复「交付流程与交期说明」；「材质对比」「按颜色浏览」「场景化推荐位」**维持否决** | 09-08 |
| A16 | 设计 Token | 由 P1 提到 **P0**，排在 v0.2.2 所有页面改造之前 | 09-09 |
| A17 | 规格选择行为 | **采纳建议稿 T134**：选新值时自动清除冲突属性 + 提示待选维度 + 提供"重新选择规格"入口。**这是对 v0.2.0 已上线 T110「再次点击取消维度」的显式回退**，须在 v0.2.3 文档中记为决策变更 | 09-09 |
| A18 | 购物车删除撤销 | **不做**。增加不必要的工作量与 bug 面 | 09-09 |
| A19 | SKU 图片联动主图 | 归 **v0.4.0**，与 3D 换色预览一并联动升级 | 09-09 |
| A20 | 端到端测试 | **引入 Playwright**，归 v0.2.3 末尾（等购物路径定型再写）。上手文档见 `docs/guides/PLAYWRIGHT-GETTING-STARTED.md` | 09-09 |
| A21 | 建议稿 P1 项 | **全部实现**，归 v0.2.3 | 09-09 |
| A22 | 结算币种 | **暂时仅人民币**。多币种等需求量明确后再拓展，本阶段不出设计说明 | 09-09 |
| A23 | 个人信息出境 | 由**用户协议 + 明示同意**处理，任务归 **v0.4.0**。见 §E6 的补充提醒 | 09-09 |
| A24 | 3D 渲染标定 | v0.4.0 第一个任务必须是渲染标定，且需先定义客观判定标准 | 09-09 |
| A25 | 支付宝沙箱 | 当前无问题。备案对生产商户号的影响放 **v0.2.3** 核实 | 09-09 |
| A26 | **产品定位（重大变更）** | 由「3D 打印摆件 / 家居 / 氛围灯」变更为 **专做电子阅读器保护壳的按单打印店**。详见 §H | 09-10 |
| A27 | 小挂件 / 小玩具 | 作为**周边**存在，不进一级导航、不占首页主要篇幅。定位模糊比定位小众危险 | 09-10 |
| A28 | 「全覆盖」的含义 | **机型库全覆盖 ≠ 全部开模**。机型库负责收流量与需求，商品负责变现，意向登记是转换器 | 09-10 |
| A29 | 开模优先级 | **无固定优先级，由需求量决定**。意向登记数就是排期依据 | 09-10 |
| A30 | 品牌名 | 建议 **书衣 / Bookskin**（古语，指包书的封套；阅读器是电子书，壳即其衣）。**待商标检索确认**：第 9 类电子设备配件、第 18 类箱包皮具 | 09-10 |
| A31 | v0.2.2 版本边界 | 由「无数据模型变更」改为「**新增机型库相关表，不触碰交易链路**」。理由：首页设备选择器、机型 SEO 落地页、意向登记全部依赖机型数据，守住零改表会导致首页做两遍 | 09-10 |
| A32 | 首页 H1 文案 | 定为 **「量卷裁衣」**（化用「量体裁衣」，卷即书卷/阅读器），副标题「为每一台电子阅读器，做一件合身的壳。新品到停产旧机，按单打印，一台也做。」。**H1 不含品类关键词是有意识的取舍**，关键词由副标题、title、description、SEO 正文四处补偿 | 09-10 |
| A33 | 首页视觉风格 | 参考「极简科技风」骨架（大留白、素色底、卡片网格），**不采用街头潮流风与通用电商模板风**。具体配色与字体**本期不写入文档**，留待实施时定 | 09-10 |
| A34 | **交期口径** | **7 天（自然日）**，自下单起算，覆盖排产打印、去支撑打磨、装机复核与发货。须在首页、机型页、商品页、结算页四处保持一致 | 09-10 |
| A35 | 装机复核 | **确认可执行，作为正式对外承诺**。每一单都必须真的装机试过。产能变化时须先改文案再调流程 | 09-10 |
| A36 | 品牌覆盖 | **阅星瞳、Kindle、掌阅、文石（BOOX）**，其余品牌按意向登记情况陆续补充 | 09-10 |
| A37 | 「书衣」商标 | **检索已完成：该商标已被注册。经评估后仍决定沿用「书衣」**，理由是当前经营规模小、风险敞口有限。**这是一项已知并接受的风险，不是遗漏。** 触发条件与后果见 §I | 09-10 |
| A38 | 品牌文案 | `docs/v0.2.2/BRAND-COPY.md` **已于 2026-09-10 定稿**，v0.2.2 的文案开工门禁已解除 | 09-10 |
| A39 | v0.2.2 部署 | **已于 2026-09-12 部署预生产**，线上 `v0.2.2+sha-718fc0d`。剩余两项人工验收：支付/登录导航回归、Google Rich Results | 09-12 |
| A40 | **商品数据与定位脱节（待处理）** | 线上唯一商品仍是旧定位的「几何氛围灯」，四个机型页均无商品。**需二选一**：尽快上架首批保护壳，或先下架氛围灯改为「正在准备第一批」。空着比答非所问好 | 09-12 |
| A41 | **新增 v0.2.3：支付宝生产通道真实性验证** | 不再推测可行性，**直接用 0.01 元真实订单实测**。方式：1 元商品 + 99% 减免折扣码 + 单省零运费规则。见 `docs/v0.2.3/CHANGES-v0.2.3.md` | 09-12 |
| A42 | 版本序列调整 | 原 v0.2.3（购物路径体验）**顺延为 v0.2.4**；原 v0.3.0（支付合规完整方案）**延后**，待 v0.2.3 实测结论出来后重新定范围；v0.3.1 退款重构、v0.3.2 邮件通知不变 | 09-12 |
| A43 | 折扣率填法（易错） | `percentage` 类型的 `discountValue` 是**折扣率**不是减免比例：代码为 `rate = 1 - discountValue`，校验要求 `0 < value < 1`。**「减免 99%」应填 `0.01`**，不是 0.99 也不是 99 | 09-12 |
| A44 | 测试订单的零运费做法 | **新增一条只覆盖单一省份、`firstAmount = 0`、`sortOrder` 最小的运费规则**，测试地址填该省。❌ 不得修改现有兜底规则——那会影响全站所有订单 | 09-12 |
| A45 | 支付配置切换无需重建镜像 | `ALIPAY_*` 全是服务端变量，不进浏览器 bundle。改 `/opt/3d-printer-site/.env` + 重启容器即可，回滚同理。与 `NEXT_PUBLIC_SITE_URL`（构建期参数）不同 | 09-12 |
| A46 | **支付路线最终决策** | 走 **青岛大小象互联网有限公司** 主体的企业支付 + ICP 备案。放弃境外主体跨境收单，也放弃个人码免签。理由见 §J | 09-13 |
| A47 | **v0.2.3 作废** | 「0.01 元真实订单实测现有形态可否收款」的命题已不成立——答案明确为否。文件保留追溯，可复用内容已迁出 | 09-13 |
| A48 | 版本序列（再次调整） | **v0.3.0 = 申请周期内的功能完善**（范围见 §K）；原 v0.2.4 购物路径体验、退款重构、邮件通知按 §K 重新分配 | 09-13 |
| A49 | 经营范围 | ✅ **已含「日用百货销售」**，支付宝签约的经营范围前置条件已满足 | 09-13 |
| A50 | 备案服务器 | 已有一台**上海 2 核 2G** 实例，**已确认支持 ICP 备案**。发布策略见 §L2 | 09-14 |
| A51 | **v0.2.3 版本号复用** | 原「支付宝生产通道验证」作废（A47），版本号复用于 **「境内迁移与跨境延迟验证」**：跨境延迟实测 + 应用迁上海 + 备案配合 + 域名切换。**不改表、不改业务逻辑** | 09-14 |
| A52 | 版本序列（再调整） | 原属 v0.3.0 的「应用迁境内」**前移至 v0.2.3**；**v0.3.0 收窄为纯业务功能**（退款引擎重构 + 订单邮件通知）；购物路径体验顺延 v0.3.1。基础设施与业务功能分开，二者的验收方式与回滚代价完全不同 | 09-14 |
| A53 | **L1 延迟首批结果（下午时段）** | 上海热连接 p95 **68.06 ms**（门禁 <80ms，过关）；**冷连接 p95 505.58 ms**；新加坡热连接 p95 4.97 ms，跨境净增量 ~63 ms。~~尚未完成上午与晚高峰采样，非最终结论~~ **（09-17 更新：三时段采样已于 09-15 补全，提交 `71cd0f7`；热连接 p95 72.74/68.06/76.31 ms 全部 <80ms，L1 最终结论为正常，见 `docs/ops/LATENCY-BASELINE.md` §4）** | 09-15 |
| A54 | 冷连接成本的工程含义 | 冷连接 505ms 意味着**生产必须复用数据库连接**，且**容器重启后首批请求会显著变慢**。发布后的健康检查与首批用户体验需据此预期；v0.3.1 的 cron 邮件派发若每次新建连接，每轮将额外付出约 0.5s | 09-15 |
| A55 | **v0.3.0 范围收窄为纯退款** | 订单邮件通知移至 v0.3.1，购物路径体验顺延 v0.3.2。理由：退款是动钱与库存的版本，出错代价最高，范围应保持最小 | 09-15 |
| A56 | A40 已解决 | 旧定位的「几何氛围灯」已下架，X3 保护壳已上架（提交 `e9c9b11`、`d25ba5f`） | 09-15 |
| B4 | ⚠️ **备案域名后缀待定** | 已注册 `daxiaoxiang.top`。`.top` 可以备案，但**被 Spamhaus / 投递性研究列为高滥用 TLD**，会直接伤害本项目的邮件投递与品牌信任。**建议改用 `.cn` / `.com.cn`**，详见 §L1。现在换成本接近零 | 09-13 |
| A57 | **L2/L3 结果：触发暂停迁移门禁** | 上海真实登录购物车 p95 **884.99 ms**、创建订单 p95 **2972.97 ms**（超过 2 秒强制改造线）、完整下单链路 p95 **6295.60 ms**（提交 `9537c2c`、`bcbe424`、`0683e4e`）；「10 抢 5」并发正确性复测仍通过、无超卖。**正式 DNS/公网流量迁移暂停，新加坡继续承载现网，上海仅保留 loopback 预部署**。恢复迁移前需在方案一（下单事务合并为单个存储过程 + 购物车查询批量化）与方案二（数据库迁入中国大陆）之间决策，见 `docs/ops/LATENCY-BASELINE.md` §7 与 `docs/v0.2.3/ACCEPTANCE-v0.2.3.md` §6 | 09-16 |

---

## B. 未决 — 归属 v0.2.2

| # | 事项 | 状态 |
|---:|---|---|
| B1 | 品牌文案逐条定稿 | **清单已出**：`docs/v0.2.2/BRAND-COPY.md`，待产品负责人二次编辑。**未定稿不得开工 T122/T124/T126/T131** |
| B2 | 交期承诺口径 | 已认同：首页、商品页、结算页三处必须一致，措辞须与实际生产能力匹配。落在 BRAND-COPY 第 4 节 |
| B3 | H13 构建产物二次核对 | 已进任务清单 **T144**，核对方法见 §D |

---

## C. 未决 — 归属 v0.2.3

| # | 事项 | 状态 |
|---:|---|---|
| C1 | 移动端购买栏范围 | **建议：只在商品详情页做常驻底部购买栏**。购物车与结算页不做常驻栏，改用页内 sticky 汇总条。理由见 §G。待确认 |
| C2 | 备案对支付宝生产商户号的影响 | 需实际核实，见 §E1 |

> C3–C6 已在本轮定案，移入 §A（A17 规格选择、A18 不做撤销、A19 SKU 图片、A20 Playwright、A21 P1 项）。

---

## D. H13 二次核对方法（v0.2.2 T144 执行）

`deploy/release.sh` 已具备蓝绿发布能力（blue/green 槽位 + healthcheck + nginx upstream 切换 + 失败自动回滚），本项核对的是**构建产物内容**而非发布方式。

三条独立证据，任一不符即说明线上跑的不是新构建产物：

1. **健康接口版本**
   `curl -s https://printer.daxiaoxiang.com/api/health` → 返回的 `version` 应为 `v0.2.2+sha-<40位>`。

   > ⚠️ **修正（2026-09-12）**：原写法「构建 SHA 应等于当前分支 HEAD」**在本项目的合并策略下不成立**。
   > 实测线上 v0.2.1 镜像的 commit 是 `28d3e89`（位于 `origin/release-v0.2.1`），而 master 上的对应提交是 `819ce6c`——
   > 两者是不同的 commit 对象，说明发布分支是以 rebase / squash 方式并入 master 的。
   > 因此**发布镜像的 SHA 永远不会等于 master 的 HEAD**。
   >
   > 正确的核对方式是二选一：
   > - 核对镜像 SHA 等于**发布时所在的那个发布分支**的 HEAD（而非 master）；
   > - 或以 `version` 字段的版本号前缀（`v0.2.2+`）作为主判据，SHA 仅用于与 `.active-release` 交叉比对。
2. **服务器活跃镜像**
   `cat /opt/3d-printer-site/.active-release` → `ACTIVE_IMAGE` 的 `sha-` 后缀应与上一条一致。
3. **浏览器产物内的站点地址**
   线上 `_next/static` 的 JS 中应出现 `https://printer.daxiaoxiang.com`，
   **不应**出现 `188.239.16.176` 或任何 `http://` 旧地址。

> `.github/workflows/release-image.yml` 写法为 `${{ vars.PREPROD_SITE_URL || 'https://printer.daxiaoxiang.com' }}`，
> 即便仓库变量未设置，回退值也是正确的 HTTPS 域名。因此 H10 未配置不会导致构建出错误地址，
> 但仍建议显式设置，避免将来域名变更时踩坑。

---

## E. 战略级（无归属版本，需并行推进）

### E1 支付宝生产商户号与「扫码轮询」模式

**现状**：代码使用 `alipay.trade.page.pay`（电脑网站支付）+ `alipay.trade.query`（主动查询）+ `alipay.trade.refund`。异步通知已在 v0.2.1 验收通过，且已加入漏投补偿（commit `b475509`）。

**关于「申请支付链接 → 二维码扫码 → 前端轮询 → 后端查询支付宝状态」这一模式的评估**：

- 技术上仍可行，对应 `alipay.trade.precreate`（当面付 / 扫码支付），属于**独立签约的产品**。
- 它解决的问题是**异步通知到不了服务器**（无公网域名、回调被拦截）。而本项目已有 HTTPS 公网域名，H16 已验收通知可直达，并且主动查询补偿已经吸收了该模式的主要好处。
- **它不解决资质问题**：无论 `page.pay` 还是 `precreate`，都需要支付宝开放平台的生产商户号，该商户号所需的企业资质与网站要求一样都要满足。
- **结论：不建议为此改造。** 现有「异步通知为主 + 主动查询补偿」的组合已经覆盖了该模式能带来的可靠性收益，且复杂度更低。

**待核实（v0.2.3）**：境外域名 / 无 ICP 备案对支付宝生产商户号审核的实际影响。以支付宝开放平台当前官方说明为准。

### E2 支付方式扩展路线

**架构侧已就绪**：`payments.provider`（`varchar(30)`）已存在，`src/lib/services/payment/` 下已有 `provider.interface.ts`、`provider.factory.ts`。**接新支付方式零改表**，只是新增 Provider 实现 + 环境变量。这是 v0.1 设计约束（订单/库存 service 禁止引用支付宝 SDK 字段）的兑现。

**业务侧卡点在资质**：国内支付通道（微信支付国内商户号、银联商户、支付宝国内商户号）普遍要求营业执照 + 已备案域名。

若走跨境通道，优先级建议：

1. **微信支付（跨境）** —— 第二个应接入的通道。两个前提：一个 `mch_id` 对应**一种结算币种**；需要**微信公众号**才能取得 APPID。资质细则需联系微信支付跨境业务团队确认。
2. **银联国际 / 云闪付** —— 边际收益小得多，主要价值在信用卡受理，排在微信之后。
3. **Stripe / PayPal** —— 属于国际化阶段，当前已推迟（A1）。

> 方向性判断，非资质结论。各通道准入条件会调整，办理时以官方当前说明为准。

### E3 ⚠️ 备案与服务器位置的认知更正

**"重新申请域名并绑定"不能替代迁移。**

ICP 备案的核心要求是**网站接入的服务器必须位于中国大陆境内**，备案是「域名 + 境内服务器实例」的绑定关系。华为云确实提供域名服务，也能代办备案，但备案时需要绑定一个**境内**实例并使用其备案服务号。换一个新域名不改变服务器在新加坡这个事实，因此**换域名解决不了备案问题**。

因果链是固定的：

```
想要国内支付通道 → 需要生产商户号 → 需要备案域名 → 需要境内服务器
```

**但"迁移"未必像想象中那么重**。存在一个明显更轻的折中方案：

> **只迁应用，不迁数据库。** 应用部署到境内实例并完成备案，Supabase 继续留在新加坡。

代价是跨境数据库延迟。`TECH_SPEC.md` §13.2 与 `CHANGES-v0.2.0.md` §10 已经讨论过应对方式：把下单事务合并为单个存储过程，减少跨境往返次数。相比"新建境内 PostgreSQL + 对象存储 + Auth 实例 + `pg_dump`/`pg_restore` 全量迁移"，这条路径的工作量小一个量级。

**建议**：在决定"不迁"之前，先按这个折中方案重新评估一次成本。

### E4 上海实例备案资格

若采纳 E3 的折中方案，须先在华为云控制台核实 Flexus L 实例是否满足备案条件——部分规格不支持，不满足则需更换为标准 ECS。**这决定境内服务器的采购规格，建议尽早确认。**

### E5 多币种定价

`payments.currency` 已存在，但商品定价是单币种。已决 A22：暂时仅人民币结算，需求量明确后再拓展。届时需要独立价目表（product_prices by currency）+ 汇率来源与更新频率。

### E6 个人信息出境合规

已决 A23：由用户协议 + 明示同意处理，任务归 v0.4.0。

⚠️ **补充提醒**：明示同意是《个人信息保护法》对个人信息出境的**必要条件之一，但通常不是充分条件**。出境还可能涉及单独同意、告知境外接收方信息、个人信息保护影响评估等要求，具体适用取决于数据规模与类型。本条不构成法律意见，正式运营前建议咨询专业人士。

---

## F. 版本归属速查

| 版本 | 主题 | 改表 |
|---|---|---|
| v0.2.2 | **机型库 + 意向登记 + 前台地基 + 首页重构 + 机型页 SEO** + 文案集中化 | **是（仅机型相关表）** |
| v0.2.3 | 购物路径体验（机型筛选 / 规格 / 购物车结算 / 订单详情 / 账户页）+ 移动端与 a11y + P1 项 + Playwright + 备案影响核实 | 否 |
| v0.3.0 | 退款引擎重构（商品级）+ 客户端退款申请 + 打印队列联动 | 是 |
| v0.3.1 | 邮件通知能力 + **意向登记的开模通知** | 是 |
| v0.4.0 | 3D 实时换色预览 + SKU 图片联动 + 个人信息出境合规协议 | 待定 |

---

## G. 移动端购买栏范围（建议，待确认）

**建议：只在商品详情页做常驻底部购买栏。**

| 页面 | 建议 | 理由 |
|---|---|---|
| 商品详情 | **做常驻底部栏**，展示当前 SKU 价格 + 主操作 | 页面长、需要滚动看图文详情，用户选完规格后主操作可能已滚出视野。这是常驻栏收益最大的场景 |
| 购物车 | **不做常驻栏**，用页内 sticky 汇总条 | 购物车本身就是短页面，主操作天然靠近内容底部 |
| 结算页 | **不做常驻栏**，用页内 sticky 汇总条 | 结算页有大量表单输入，移动端键盘弹起时常驻底部栏会被顶起或遮挡输入框，是典型的体验陷阱 |

**共同要求**：适配 iPhone 安全区域（`env(safe-area-inset-bottom)`），避免被 Home 指示条遮挡。

三个页面都做常驻栏会让整站看起来像一个"到处是浮动条"的廉价模板，而且结算页的键盘冲突会实实在在制造 bug。

---

## H. 产品定位（A26 全文）

> **一家专做电子阅读器保护壳的按单打印店。**
> 覆盖阅星瞳、Kindle、掌阅、文石等主流品牌，也做已停产、大厂不再供货的老机型。
> **下一个开哪个机型，由需求量决定**——用户可登记意向，够人要就开模。

### H1 三层结构

| 层 | 内容 |
|---|---|
| 品类 | 电子阅读器保护壳（小挂件小玩具为周边，见 A27） |
| 差异 | 冷门机型与停产老款也有；按单打印，不压库存 |
| 参与 | 没有你的型号可以登记，需求量决定开模顺序 |

### H2 为什么这个定位成立

三个特征叠加，几乎是为 3D 打印量身定做的：

1. **机型碎片化到荒谬**——每个型号尺寸不同，壳不通用
2. **每个型号的市场都太小**——注塑开模要卖上千个才回本，厂商只做最热门的几款
3. **用户痛感强且无替代**——冷门型号和停产老款的用户往往全网买不到合适的壳

**护城河不是「我们按单生产」，而是「只有我们做这个型号」。** 按单生产退居第二位，作为支撑理由存在。

### H3 意向登记是引擎，不是附加功能

长尾生意最难的问题是「先做哪个 SKU」。意向登记一次解决三件事：需求验证、首批买家、**持续更新的内容供给**（投票榜、开模进度、新机型上线——这比任何前端花招都更能解决「首页新鲜度」问题）。

### H4 用户行为特征

用户**带着确定目标来**：他有一台什么机器，就要那台机器的壳。因此首页第一屏的核心是**设备选择器**，不是商品网格；站内 SEO 主力是**机型落地页**，不是商品列表。

### H5 已知的连带影响

| 影响面 | 说明 | 归属 |
|---|---|---|
| SEO 关键词 | 从「3D打印摆件」改为**机型名长尾词**（如「阅星瞳 X4 保护壳」），竞争极低、意图极强 | v0.2.2 |
| 首页结构 | Hero 从「文案 + banner」改为「文案 + 设备选择器」 | v0.2.2 |
| 数据模型 | 新增 `device_brands` / `device_models` / `product_device_models` / `model_requests` | v0.2.2 |
| 商品筛选 | 第一维度是「品牌 → 型号」层级选择，不是价格与颜色 | v0.2.3 |
| **退款规则** | **外壳是精密配合件，「装不上」投诉率远高于摆件。A10 的「开始打印后不允许客户申请退款」需增加尺寸不符的例外情形** | v0.3.0 |
| 耗材选型 | PLA 做外壳偏脆且夏季易变形，PETG / TPU 更合适；影响 BOM 与商品页参数 | 上架前 |
| 命名合规 | 可写「适用于 {品牌}{型号}」；不得用对方 logo、不得命名为让人误认为官方产品、不得逆向复制原厂壳外观 | v0.2.2 T155 |
| 错误码 | v0.2.2 占用 40913 / 40914，**v0.3.0 的退款错误码顺延至 40915 起** | v0.3.0 |

### H6 延展机会

阅星瞳是**磁吸式**设备。磁吸生态可延展出支架、卡包、握把、配重等配件——这类配件同一用户可买多件，复购价值可能高于保护壳本身。X4 已上市，新机型上市初期是配件需求最旺、供给最少的窗口。

### H7 thin content 风险（重要）

机型页策略是「未开模机型也建页，抢占长尾词并收集需求」。但**大批只有标题、没有实质内容的页面会被判定为 thin content 或 doorway page，可能牵连整站权重**。

每个机型页必须有真内容：设备尺寸参数、接口位置、当前登记人数、相同尺寸可共用的其他机型、相关已开模商品。**页面可以没有商品，但不能没有内容。**

---

## I. 「书衣」商标风险（A37 详述）

### I1 已确认事实

「书衣」商标**已被他人注册**。经评估后决定沿用，属于**主动接受的风险**。

### I2 需要澄清的一处认知

决策讨论中提出「独立站不影响国内商标问题」。这一表述**并不准确**，记录在此以免后续基于该前提做进一步判断：

商标权确有地域性，但侵权认定看的是**是否在中国境内进行商业使用**，而非服务器位于何处。本站面向中国大陆用户、以人民币计价、通过支付宝收款、向大陆发货——这些事实在实践中通常足以构成境内商业使用。**服务器在新加坡不构成免责事由。**

真正成立的理由是另一条：**当前经营规模小，权利人主动维权的概率低**。这是一个合理的商业取舍，但它的成立依赖「规模小」这个前提——前提变化时结论需要重估。

### I3 风险触发条件

出现以下任一情况时，应重新评估是否更名：

1. 销量或站点知名度明显上升
2. 开始在国内平台投放推广（小红书、抖音、微博等）——曝光会显著提高被发现的概率
3. **SEO 开始见效**——注意这与 v0.2.2 的核心投入直接冲突：机型长尾词做起来的过程，本身就是提高被权利人发现概率的过程
4. **申请支付宝生产商户号时**——商户号审核需提交经营与品牌信息，与在先商标冲突可能影响审核结果。这条与 E1、E2 的既有规划直接相关

### I4 成本曲线

改名成本随时间急剧上升：

| 时点 | 成本 |
|---|---|
| **现在** | 近乎为零——改几份文档，代码尚未实现 |
| SEO 收录后 | 域名、外链、收录、站内文案全部重做，已积累的搜索排名归零 |
| 有老客户后 | 再加上品牌认知迁移与客户沟通成本 |

### I5 备选名（若将来需要更名）

均为生造组合词，非现成词汇，商标通过率相对更高：

| 名称 | 内涵 | 英文 |
|---|---|---|
| 抱卷 | 双手护着一卷书，即壳护着阅读器；与 H1「量卷裁衣」共用「卷」字 | Bookhold |
| 函衣 | 「函」为书函，「衣」为外覆 | Bookcase |
| 承卷 | 承托书卷 | Bearer |

> 不要走「书衣社」「书衣室」这类加后缀的路径——商标近似审查中通常仍会被判定与在先商标近似。

---

## J. 支付路线决策（A46 全文）

### J1 三条路线的比较

| 路线 | 前提 | 判断 |
|---|---|---|
| **境内企业支付 + ICP 备案** ✅ 采纳 | 营业执照（已有）+ 经营范围含销售 + 备案域名 + 境内服务器 | 主体现成，费率低（~0.6%），资金直接进公司账户可正常记账开票 |
| 境外主体 + 跨境收单（Antom / Alipay Global，或经 Stripe、Airwallex、PingPong 等 PSP） | 境外注册公司 + 境外银行账户 | 不需要备案，但要新注册境外公司；费率 2–3%；资金回流麻烦。**且实际经营、发货、人员均在境内，可能被认定为境内应税，境外主体拿不到税务上的好处** |
| 个人码 + 免签监控 | 无 | ❌ 已排除。违反支付宝个人账户协议，且 2022 年央行《关于加强支付受理终端及相关业务管理的通知》后，个人静态码用于经营收款属监管禁止；无退款 API 会打断 `refund.service`；靠金额匹配识别订单存在碰撞问题 |

### J2 采纳境内路线的关键理由

这是一门**彻头彻尾的境内生意**——商品从境内发货、用户是境内消费者、经营者在境内。用境外通道收款是把简单的事做复杂。

且总成本更低：经营范围增项几百元几天 + 境内最小实例几十元每月 + 备案 7–20 工作日（免费），对比新注册并维护一家境外公司。

### J3 待办动作（申请侧，与开发并行）

1. **工商增项经营范围**——公司名含「互联网」，经营范围大概率不含销售类目。支付宝审核可能以「经营范围与网站经营内容不符」驳回，且开不了货物销售发票。建议加「日用百货销售」「电子产品销售」「互联网销售」。**此步应先于支付宝申请完成**
2. **核对域名持有人**——`daxiaoxiang.com` 若注册在个人名下而备案主体为公司，需提供域名证书证明持有关系，这是常见卡点
3. **境内实例 + 应用迁移 + 备案**——见 §K
4. **企业支付宝签约**

### J4 已知的连带义务

| 项 | 说明 |
|---|---|
| 税务 | 货款进公司账户即公司收入，需申报纳税并开票。货物销售与技术服务的税率、科目均不同；资金从公司支出另涉分红个税 |
| 主体风险 | 售后纠纷、消费者投诉、行政处罚的承担主体是公司，会牵连其他业务与公司信用 |
| 网站展示 | 页脚须展示 ICP 备案号与经营主体信息，「青岛大小象互联网有限公司」将出现在「书衣」站点上 |
| **七天无理由退货** | 已多次讨论并由产品方决定「不提供」。**此处仅作事实留痕**：站点销售的是预设 SKU 而非消费者定作商品，法定不属于《消法》第 25 条的除外情形；商家单方声明经法院认定不成为合同内容。合法限制退货的唯一路径是「在销售必经流程中明确提醒 + 消费者主动确认」。切换为企业主体后，消费者可通过支付宝与 12315 投诉，且承担主体为公司，风险高于个人经营时。**工程上的建议：退款准入规则做成可配置，不要把「不允许退款」硬编码** |
| ICP 经营许可证 | 自营电商属非经营性网站，**通常不需要** EDI 许可证。该许可证针对为第三方提供交易平台的情形 |

> 本节不构成法律意见。正式办理前建议咨询专业人士。

---

## K. v0.3.0 范围（A48）

> 状态：**建议稿，待确认**

定位：**在资质与备案审批周期内（约 3–5 周）完成功能完善，使支付通道到位当天即可开卖。**

判断标准只有一条：**支付到位的那一天，还差什么才不能开门做生意。**

### K1 建议范围（三块）

| # | 内容 | 为什么在这一版 |
|---:|---|---|
| ~~一~~ | ~~应用迁境内 + 备案配合~~ | **已前移至 v0.2.3**（A52） |
| **二** | **退款引擎商品级重构** | 收真钱后第一个客诉就用得上。现状是订单级、只能后台发起，且**部分退款不返还库存**（`restock: isFullRefund && input.restock`） |
| **三** | **订单状态邮件通知** | 按单生产有 7 天静默期，用户付款后全程失联是客诉高发区。发信通道正好与备案、SMTP 配置一并落地 |

### K2 第一块的关键风险：跨境数据库延迟

方案是**只迁应用，Supabase 留新加坡**。这意味着每次数据库往返都要跨境。

v0.2.0 曾把接口耗时从 2.5s 优化到 0.67s，但那是**本地访问新加坡 Supabase** 的数据。迁到境内服务器后延迟特性会变——可能更好（机房线路优于家宽），也可能更差。

**必须实测，不得假设。** `TECH_SPEC.md` §13.2 的「下单事务合并为单个存储过程」是备选缓解方案，**先测再决定是否投入**，不要预先按最坏情况施工。

### K3 不在这一版

- 购物路径体验（机型筛选、规格选择、购物车与结算、订单详情、账户页、移动端 a11y、P1 项、Playwright）→ v0.3.1
- 3D 实时换色预览 → v0.4.0
- 商品上架与 A40 处理 → **运营动作，与本版本并行，不占版本范围**

### K4 时间紧时的取舍

若审批比预期快，或开发资源不足，**第三块（邮件通知）可以砍到 v0.3.1**——站内订单中心已能查看状态，邮件是体验加分而非功能缺失。

**第一块和第二块不可砍**：前者是合规前置，后者是收真钱的安全底线。

---

## L. 境内迁移的三个待确认项（2026-09-13）

### L1 ⚠️ 备案域名后缀：建议不要用 `.top`

`.top` **可以备案**（在工信部批复的可备案后缀列表内），这一点没问题。问题在别处。

`.top` 是全球滥用率最高的 TLD 之一。邮件投递性研究将其列为 **"very high" 风险并建议完全避免**用作发信域名；推荐的生产发信域名是 `.com`、`.net`、`.org` 或匹配的国别后缀。

对本项目的具体伤害有三层：

| 影响面 | 说明 |
|---|---|
| **邮件投递（最直接）** | 登录 OTP 邮件此前已出现进垃圾箱的问题；v0.3.0 第三块还要新增订单状态通知。用 `.top` 做发信域名等于在已有问题上再加一层惩罚性评分——收件方在看内容之前就先扣分 |
| **SEO 与点击率** | TLD 本身不直接影响排名，但 `.top` 的整体声誉差，搜索结果里用户点击意愿低。而机型长尾词是本站 SEO 的全部依据 |
| **用户信任** | 国内消费者对 `.top` 电商站普遍存疑。与「书衣」要建立的克制、可信的品牌调性冲突 |

**建议改用 `.cn` 或 `.com.cn`**：境内后缀备案最顺、声誉正常、对国内电商显得正规、价格低廉。

**现在换的成本接近零**——尚未备案、尚未上线、尚未积累任何 SEO 收录或外链。一旦用 `.top` 完成备案并上线，再换就要重走备案流程并承担收录与认知的迁移成本。

### L2 上海服务器待确认

| # | 项 | 状态 |
|---:|---|---|
| 1 | 是否支持 ICP 备案 | ✅ **已确认支持** |
| 2 | 2 核 2G 的发布策略 | 内存预算：OS ~300MB + Docker ~120MB + Nginx ~30MB + 应用容器 768MB ≈ 1.2GB / 2GB，**日常运行够用**。但蓝绿发布需要同时容纳两份容器（~1.5GB），2G 装不下，因此**发布时只能「先停后起」，有数十秒停机**。早期可接受，选在低峰时段发布即可；有流量后再升配 |

### L3 🔥 最该立刻做的一件事：实测跨境数据库延迟

应用迁上海、Supabase 留新加坡 `ap-southeast-1`，**每次数据库往返都要跨境**。这是本次迁移最大的未知数。

v0.2.0 曾把接口耗时从 2.5s 压到 0.67s，但那是**本地访问新加坡 Supabase** 的数据，与「上海机房 → 新加坡」是完全不同的链路。

**现在机器已经到位，这个测试十分钟就能做完，且结果直接决定要不要投入做 `TECH_SPEC.md` §13.2 的存储过程合并。**

第一步（裸延迟）：在上海服务器上对 Supabase 连一串单次往返，取 p50 / p95。

第二步（真实负载）：应用部署后直接看**既有的 `Server-Timing` 响应头与 >500ms 慢请求日志**（v0.2.0 的 T115 已经建好这套观测，不需要额外工具）。

判断口径建议：

| p95 单次往返 | 结论 |
|---|---|
| < 80ms | 正常，**不需要**做存储过程合并 |
| 80–150ms | 可用，但下单等多次往返的事务需要观察 |
| > 150ms 或抖动明显 | 需要按 §13.2 合并下单事务，或重新考虑数据库位置 |

**先测再决定，不要预先按最坏情况施工。**

> 📄 完整需求说明已并入 **[`docs/v0.2.3/CHANGES-v0.2.3.md`](./v0.2.3/CHANGES-v0.2.3.md) 的 T160**，可直接交给执行 agent。
> 要点：三层测量（裸往返 / 真实接口 / 下单事务）、必须分时段采样、必须报 p95 而非平均值、需设新加坡对照组、需复测 v0.1 验收项 14 的并发扣料。
> **L1 不依赖应用部署，可立即开始。**
