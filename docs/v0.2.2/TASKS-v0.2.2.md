# v0.2.2 执行待办清单

> 状态：执行中  
> 更新日期：2026-09-11  
> 唯一需求基线：[`CHANGES-v0.2.2.md`](./CHANGES-v0.2.2.md)（2026-09-10 重写定稿版）  
> 文案基线：[`BRAND-COPY.md`](./BRAND-COPY.md)  
> 分支：`release-v0.2.2`

本文档按最新版需求重新生成。旧版“无数据模型变更”边界已经作废；当前边界为：**新增机型库与意向登记相关表，不触碰交易链路表。**

## 0. 状态说明与执行规则

- `[x]` 已完成并通过当前阶段自动检查。
- `[~]` 已开始或代码完成但仍有依赖/人工验收。
- `[ ]` 未开始。
- 每批任务完成后执行专项测试；提交由用户明确要求后进行。
- 数据库迁移先生成并审查，应用到环境前确认目标环境和恢复点。
- 支付宝外跳保留浏览器级导航，其他站内跳转统一使用 Next.js 导航。
- v0.2.3 的购物路径精细交互与 Playwright 不纳入本版本。

## 1. 当前基线

- [x] 当前分支确认：`release-v0.2.2`。
- [x] 实施前 HEAD 已记录：`819ce6cfca93c62a9fac28558dad0fadccee3fd5`。
- [x] 品牌文案门禁已解除，定稿日期 2026-09-10。
- [x] 已识别并保留用户已有的 README、v0.2.1 和其他文档改动。
- [x] 基线 `typecheck`、`lint`、40 项测试、生产构建、安全审计通过。
- [x] Supabase 2026-09-11 前的 changelog 与 RLS 指南已核对。
- [x] 新表采用 RLS + 撤销 `anon/authenticated` 权限，客户端不直连新表。

## 2. 推荐执行批次

| 批次 | 任务 | 状态 | 建议提交信息 |
|---|---|---|---|
| B1 | T119、T140、T141、T143、T120、T121 | `[~]` | `feat: establish v0.2.2 storefront foundation` |
| B2 | T150、T151 | `[~]` | `feat: add device catalog management` |
| B3 | T152、T153 | `[ ]` | `feat: seed devices and accept model requests` |
| B4 | T122–T126 | `[ ]` | `feat: rebuild homepage for reader cases` |
| B5 | T154、T127–T130 | `[ ]` | `feat: add device landing pages and seo` |
| B6 | T131、T142、T155 | `[~]` | `refactor: finalize storefront copy and navigation` |
| B7 | 全量验收、发布资料、T144 | `[ ]` | `docs: finalize v0.2.2 release` |

---

## B1 前台与首页数据地基

### T119 版本号与发布分支 `[~]`

- [x] `package.json` 版本更新为 `0.2.2`。
- [x] `release-image.yml` 增加 `release-v0.2.2` 触发分支。
- [x] 镜像 `APP_VERSION` 更新为 `v0.2.2+sha-${{ github.sha }}`。
- [x] 本地构建通过。
- [ ] 推送后确认 GitHub Actions 被触发。
- [ ] 确认线上 `/api/health` 返回 v0.2.2 与正确 SHA。

### T140 设计 Token 基线 `[~]`

- [x] 建立品牌颜色、状态色、圆角和阴影 Token。
- [x] 商城页面散落十六进制颜色已迁移到 Token。
- [x] 增加全站 `focus-visible`。
- [x] 增加 `prefers-reduced-motion` 基础规则。
- [x] typecheck、lint、build 通过。
- [ ] 新机型页实现后确认无散落十六进制颜色。
- [ ] 四档视口完成人工视觉检查。

### T141 加载、错误与空状态基础 `[~]`

- [x] 建立首页、列表、详情骨架屏。
- [x] 建立通用空状态组件。
- [x] 建立可重试错误状态与商城风格 404。
- [x] 首页、商品列表、分类列表、商品详情接入路由加载状态。
- [ ] 机型列表/选择器接入加载、错误与空状态。
- [ ] 机型详情接入加载、错误、空数据与未找到状态。
- [ ] 人工确认骨架屏切换无明显布局跳动。

### T143 内容图片优化 `[~]`

- [x] Supabase Storage 公共图片域名接入 Next Image 配置。
- [x] 首页分类图由 CSS 背景迁移到 `next/image`。
- [x] Hero banner 由 CSS 背景迁移到 `next/image`。
- [x] 商品卡和商品图库移除 `unoptimized`。
- [x] 3D Viewer 保持按需加载。
- [ ] 机型页内容图片接入 `next/image`。
- [ ] 人工确认首屏无明显图片布局跳动。

### T120 首页数据缓存重构 `[~]`

- [x] banner、站点信息、首页分类、精选与最新数据改用 `unstable_cache`。
- [x] 建立 banner/site/category/product cache tag。
- [x] 商品、分类、耗材和站点后台变更会失效相关缓存。
- [x] 保留首页 `force-dynamic`。
- [ ] 新增机型数据缓存与 `device` cache tag。
- [ ] 后台机型变更后触发缓存失效。
- [ ] 在数据库查询日志中验证连续访问不重复查库。

### T121 精选与最新上架去重 `[x]`

- [x] 最新集合排除精选商品。
- [x] 数量不足时不重复填充。
- [x] 保留最新排序并限制展示数量。
- [x] 新增 3 项去重单元测试。

---

## B2 机型库与后台管理

### T150 机型数据模型 `[~]`

- [x] 新增 `device_brands` schema。
- [x] 新增 `device_models` schema，包含 aliases、停产、开模、尺寸、兼容组。
- [x] 新增 `product_device_models` 多对多关联 schema。
- [x] 新增 `model_requests` schema 与唯一约束。
- [x] 新增品牌/型号/兼容组/登记查询索引。
- [x] 增加 `device:manage` 权限码。
- [x] Drizzle 迁移 `0003_handy_monster_badoon.sql` 已生成。
- [x] 新表启用 RLS，并撤销 `anon/authenticated` 全部权限。
- [x] `service_role` 权限显式保留；浏览器端不暴露服务端密钥。
- [x] 未改动订单、库存、支付、退款及生产相关 schema。
- [ ] 确认目标环境和可恢复点。
- [ ] 应用迁移并执行表、约束、索引、RLS 与权限验证查询。
- [ ] 运行现有 `test:acceptance` 验证交易链路。

### T151 机型库后台管理 `[~]`

- [x] 新增品牌与机型校验器。
- [x] 新增品牌 CRUD 服务。
- [x] 新增机型 CRUD 服务。
- [x] 新增商品—机型多对多关联维护。
- [x] 唯一约束、引用约束转换为明确业务错误。
- [x] 所有写操作与 `admin_operation_logs` 同事务。
- [x] 新增 `/api/admin/devices` 相关 API。
- [x] API 全部要求 `device:manage`。
- [x] 新增后台机型管理页面与导航入口。
- [x] 后台录入界面展示第三方品牌命名规范。
- [ ] 增加 403、CRUD、审计日志和关联维护测试。

---

## B3 数据录入与意向登记

### T152 机型数据录入 `[~]`

- [x] 准备可重复执行、默认仅预览且需显式确认写入的种子数据脚本。
- [x] 整理首批四品牌各一个机型的来源、尺寸与实机复核说明。
- [ ] 录入阅星瞳机型与别名。
- [ ] 录入 Kindle 机型与别名。
- [ ] 录入掌阅机型与别名。
- [ ] 录入文石/BOOX 机型与别名。
- [ ] 每个机型包含年份、停产状态、外形尺寸与必要说明。
- [ ] 只录入有实物或可靠资料支撑的数据。
- [ ] 通过名称、品牌别名和型号别名搜索验收。

### T153 意向登记 `[~]`

- [x] 新增登记输入校验器并统一 email 小写化。
- [x] 新增 `MODEL_REQUEST_EXISTS`（40913）。
- [x] 新增 `MODEL_REQUEST_RATE_LIMITED`（40914/HTTP 429）。
- [x] 新增同 IP 频率限制及测试重置能力。
- [x] 新增登记服务、人数聚合和唯一冲突处理。
- [x] 新增 `POST /api/model-requests`。
- [ ] 在无商品机型页展示邮箱、说明、当前人数和提交状态。
- [ ] 明确提示本期只登记，开模后通知能力将在 v0.3.1 提供。
- [ ] 验证重复登记、并发登记、IP 限流和人数准确性。

---

## B4 新定位首页

### T122 Hero 与设备选择器 `[ ]`

- [ ] Hero 固定 H1 为「量卷裁衣」。
- [ ] 副标题包含「电子阅读器」与「壳」。
- [ ] 实现品牌 → 型号两级选择。
- [ ] 实现名称及 aliases 搜索。
- [ ] 选择后进入 `/devices/[brand]/[model]`。
- [ ] 全流程可纯键盘操作且不超过 3 次操作。
- [ ] 360 / 390 / 768 / 1440 下选择器均在首屏可见。
- [ ] banner 为空或失败时选择器仍完整可用。

### T123 首页版式重排 `[ ]`

- [ ] Hero：品牌主张 + 设备选择器。
- [ ] 第二屏：冷门也有 / 按单打印 / 没有就登记。
- [ ] 已开模商品使用大卡片。
- [ ] 新开模商品使用小卡密排。
- [ ] 增加意向登记入口。
- [ ] 底部展示交付流程与 CTA。
- [ ] 各 section 形态不同，四档视口无横向滚动。

### T124 交付流程说明 `[ ]`

- [ ] 首页展示完整五步生产交付流程。
- [ ] 首页、机型页、商品页、结算页统一为「7 天内发出」。
- [ ] 明确 7 天为自然日并包含装机复核与发货。
- [ ] 保留订单集中时可能顺延的说明。

### T125 首页 SEO 正文 `[ ]`

- [ ] 使用 `BRAND-COPY.md` 约 290 字定稿正文。
- [ ] 首段出现「电子阅读器保护壳」。
- [ ] 正文使用可索引语义 HTML。
- [ ] 纯文本抓取验证核心品类词。

### T126 SectionHeading 文案 `[ ]`

- [ ] 设备选择、已开模、新开模、意向登记使用定稿标题。
- [ ] eyebrow 承载品牌/品类词，title 使用自然表达。
- [ ] 清除旧定位模板话术与重复标题。

---

## B5 机型落地页与 SEO

### T154 机型落地页 `[ ]`

- [ ] 新增 `/devices/[brand]/[model]`。
- [ ] 页面展示品牌、型号、年份、停产状态、尺寸和接口说明。
- [ ] 有商品时展示关联保护壳。
- [ ] 无商品时展示意向登记入口和当前人数。
- [ ] 展示 compat group 中可共用壳的其他机型。
- [ ] 无效品牌/型号进入 404 并引导搜索。
- [ ] 为无商品页面提供足够实质内容，避免 thin content。
- [ ] 随机抽查 10 个页面通过内容质量判断。

### T127 sitemap 与 robots `[ ]`

- [ ] sitemap 包含可见机型页、上架商品与静态页。
- [ ] 下架商品和隐藏机型不进入 sitemap。
- [ ] robots 屏蔽 `/admin`、`/account`、`/checkout`、`/api`。
- [ ] 所有绝对地址基于 `NEXT_PUBLIC_SITE_URL`。

### T128 meta 治理 `[ ]`

- [ ] 根 metadata 更新为「书衣」身份与统一后缀。
- [ ] 首页 title 含「电子阅读器保护壳」。
- [ ] 首页 description 含品类词和四个主要品牌。
- [ ] 机型页使用 `{品牌}{型号} 保护壳｜书衣`。
- [ ] 商品列表和详情补齐 title、description、canonical、OpenGraph。
- [ ] canonical 中无 localhost、IP 或 HTTP 旧地址。

### T129 结构化数据 `[ ]`

- [ ] 保留并更新 Organization、WebSite。
- [ ] 首页增加 ItemList。
- [ ] 商品页增加 Product、Offer、BreadcrumbList。
- [ ] 机型页增加 BreadcrumbList 和适用的 ItemList。
- [ ] JSON-LD 注入前把 `<` 转义为 `\\u003c`。
- [ ] 恶意文本安全测试通过。
- [ ] Google Rich Results 无阻塞错误。

### T130 可访问性与性能 `[ ]`

- [ ] 图片 alt 与语义化标题层级完整。
- [ ] 设备选择器可纯键盘操作。
- [ ] 表单错误与字段关联，异步状态使用 `aria-live`。
- [ ] 减少动画偏好生效。
- [ ] 记录设备、网络、浏览器与构建 SHA。
- [ ] LCP ≤ 2.5s、INP ≤ 200ms、CLS ≤ 0.1，或记录偏差理由。

---

## B6 文案、导航与命名规范

### T131 文案集中化 `[ ]`

- [ ] 新增 `src/messages/zh-CN.ts`，不引入 i18n 运行时。
- [ ] 抽取公共导航与通用按钮。
- [ ] 抽取交易状态。
- [ ] 抽取通用错误提示。
- [ ] 抽取交期、运费与售后口径。
- [ ] 抽取品牌、SEO 与未来邮件复用文案。

### T142 客户端导航统一 `[~]`

- [x] 商城侧站内 `window.location.assign` 已迁移到 Next.js Router。
- [x] 登录回跳统一使用 `next` 参数，并拒绝 `//` 外部回跳。
- [x] 支付宝 `payUrl` 外部跳转保持浏览器级导航。
- [ ] 后台侧站内 `window.location.assign` 全部迁移。
- [ ] 支付外跳、return 回跳、登录恢复购买完成人工回归。

### T155 命名规范落地 `[ ]`

- [ ] 后台机型与商品录入页展示“适用于 {品牌}{型号}”提示。
- [ ] 不使用第三方 logo 或官方包装视觉。
- [ ] 不采用可能误认为官方产品的商品命名。
- [ ] 审核现有商品及新录入机型数据。

---

## B7 全量验收与发布

### 自动质量门禁

- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm test`
- [ ] `pnpm test:acceptance`
- [ ] `pnpm build`
- [ ] `pnpm security:audit`
- [ ] `git diff --check`

### 21 项需求验收

- [ ] 流水线与健康版本。
- [ ] T144 构建产物三条证据。
- [ ] 交易链路不受影响。
- [ ] 别名搜索。
- [ ] 首页设备选择器。
- [ ] 机型页内容质量。
- [ ] 意向登记唯一性、限流、计数。
- [ ] 设计 Token。
- [ ] 四类页面状态。
- [ ] 站内导航与支付跳转。
- [ ] 首页缓存。
- [ ] 商品去重。
- [ ] 首页版式与四档视口。
- [ ] 四处交期一致。
- [ ] 首页 SEO 正文。
- [ ] H1 四处关键词补偿。
- [ ] sitemap 与 robots。
- [ ] 结构化数据。
- [ ] 域名正确性。
- [ ] 命名规范。
- [ ] 文案集中化。
- [ ] 可复现性能基线。

### T144 发布后构建产物核对

- [ ] `/api/health` 的 SHA 等于分支 HEAD。
- [ ] `.active-release` 的镜像 sha 后缀与健康检查一致。
- [ ] 线上 `_next/static` 包含 `https://printer.daxiaoxiang.com`。
- [ ] 线上 `_next/static` 不含服务器 IP 或 HTTP 旧地址。

发布记录：

- 最终提交：`待填写`
- 迁移版本：`待填写`
- 发布镜像：`待填写`
- 发布日期：`待填写`
- Tag：`待填写`
